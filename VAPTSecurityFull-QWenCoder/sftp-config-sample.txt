# SFTP Deployment Configuration for VAPT Security Plugin
# Copy this file to sftp-config.txt and fill in your details
# IMPORTANT: Set permissions to 600 after creating: chmod 600 sftp-config.txt

# Server hostname or IP address
HOST=your-server.com

# SSH port (default: 22)
PORT=22

# Your SSH username
USER=your-username

# Authentication method - choose ONE of the following:

# Option 1: Password authentication (less secure)
PASS=your-password

# Option 2: SSH Key authentication (recommended)
# Uncomment the line below and provide path to your private key
# KEY=/home/username/.ssh/id_rsa

# Remote WordPress installation path
# Common paths:
#   - /var/www/html/wp-content/plugins  (standard Linux)
#   - /home/username/public_html/wp-content/plugins  (cPanel)
#   - /var/www/yourdomain.com/wp-content/plugins  (custom setup)
REMOTE_PATH=/var/www/html/wp-content/plugins

# Optional: Site-specific subdirectory if using multisite
# SITE_SUBDIR=site1  # Leave empty or comment out for single site
